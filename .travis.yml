language: cpp

compiler: 
    - g++

addons:
    apt:
        packages:
            - bison
            - flex
            - llvm
            - clang

dist: trusty
sudo: required

script:
    - make 
    - ./testcases.sh
